const describe = require('mocha').describe
const it = require('mocha').it
const expect = require('chai').expect
const strictEqual = require('../operation')

describe('function', () => {
    describe('strictEqual', () => {
        it('returns the boolean of all the values provided', () => {
            //create the test data 
            const operands = [20, 50] == operand['20', '50']

            //call the function with test data
            const Boolean = Boolean(operands)

            //make assertions.what should be done 
            expect(boolean).to.deep.equal(true)
        })
    }
})
